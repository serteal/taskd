// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: admin/admin.proto

// Package admin is taskd's daemon-administration API — deliberately kept
// separate from the frozen task.TaskService. task.TaskService is the task
// data plane and is meant to be frozen; admin is the operational control
// plane, where clients change how the daemon itself runs. Today that means
// enabling and disabling extensions; it is meant to grow to also carry
// daemon settings and user preferences. Keeping the two apart means the task
// contract never has to absorb operational churn.
//
// VERSIONING: like task, this package is intentionally unversioned and
// evolves only additively — new fields, new RPCs — which proto3 clients
// ignore when unknown. Nothing is ever renamed, renumbered, or removed.
package adminconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	admin "github.com/serteal/taskd/gen/admin"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// AdminServiceName is the fully-qualified name of the AdminService service.
	AdminServiceName = "admin.AdminService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// AdminServiceListExtensionsProcedure is the fully-qualified name of the AdminService's
	// ListExtensions RPC.
	AdminServiceListExtensionsProcedure = "/admin.AdminService/ListExtensions"
	// AdminServiceSetExtensionEnabledProcedure is the fully-qualified name of the AdminService's
	// SetExtensionEnabled RPC.
	AdminServiceSetExtensionEnabledProcedure = "/admin.AdminService/SetExtensionEnabled"
)

// AdminServiceClient is a client for the admin.AdminService service.
type AdminServiceClient interface {
	// Lists every installed extension with its capabilities and whether it is
	// currently enabled. Disabled extensions are still reported (that is how a
	// client offers to re-enable one); they are simply not supervised and not
	// served.
	ListExtensions(context.Context, *connect.Request[admin.ListExtensionsRequest]) (*connect.Response[admin.ListExtensionsResponse], error)
	// Enables or disables one extension by name. Disabling stops its syncer
	// process and stops serving its web bundle; enabling reverses both. The
	// change is persisted to the daemon's config and applied to the running
	// daemon. See restart_required.
	SetExtensionEnabled(context.Context, *connect.Request[admin.SetExtensionEnabledRequest]) (*connect.Response[admin.SetExtensionEnabledResponse], error)
}

// NewAdminServiceClient constructs a client for the admin.AdminService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAdminServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AdminServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	adminServiceMethods := admin.File_admin_admin_proto.Services().ByName("AdminService").Methods()
	return &adminServiceClient{
		listExtensions: connect.NewClient[admin.ListExtensionsRequest, admin.ListExtensionsResponse](
			httpClient,
			baseURL+AdminServiceListExtensionsProcedure,
			connect.WithSchema(adminServiceMethods.ByName("ListExtensions")),
			connect.WithClientOptions(opts...),
		),
		setExtensionEnabled: connect.NewClient[admin.SetExtensionEnabledRequest, admin.SetExtensionEnabledResponse](
			httpClient,
			baseURL+AdminServiceSetExtensionEnabledProcedure,
			connect.WithSchema(adminServiceMethods.ByName("SetExtensionEnabled")),
			connect.WithClientOptions(opts...),
		),
	}
}

// adminServiceClient implements AdminServiceClient.
type adminServiceClient struct {
	listExtensions      *connect.Client[admin.ListExtensionsRequest, admin.ListExtensionsResponse]
	setExtensionEnabled *connect.Client[admin.SetExtensionEnabledRequest, admin.SetExtensionEnabledResponse]
}

// ListExtensions calls admin.AdminService.ListExtensions.
func (c *adminServiceClient) ListExtensions(ctx context.Context, req *connect.Request[admin.ListExtensionsRequest]) (*connect.Response[admin.ListExtensionsResponse], error) {
	return c.listExtensions.CallUnary(ctx, req)
}

// SetExtensionEnabled calls admin.AdminService.SetExtensionEnabled.
func (c *adminServiceClient) SetExtensionEnabled(ctx context.Context, req *connect.Request[admin.SetExtensionEnabledRequest]) (*connect.Response[admin.SetExtensionEnabledResponse], error) {
	return c.setExtensionEnabled.CallUnary(ctx, req)
}

// AdminServiceHandler is an implementation of the admin.AdminService service.
type AdminServiceHandler interface {
	// Lists every installed extension with its capabilities and whether it is
	// currently enabled. Disabled extensions are still reported (that is how a
	// client offers to re-enable one); they are simply not supervised and not
	// served.
	ListExtensions(context.Context, *connect.Request[admin.ListExtensionsRequest]) (*connect.Response[admin.ListExtensionsResponse], error)
	// Enables or disables one extension by name. Disabling stops its syncer
	// process and stops serving its web bundle; enabling reverses both. The
	// change is persisted to the daemon's config and applied to the running
	// daemon. See restart_required.
	SetExtensionEnabled(context.Context, *connect.Request[admin.SetExtensionEnabledRequest]) (*connect.Response[admin.SetExtensionEnabledResponse], error)
}

// NewAdminServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAdminServiceHandler(svc AdminServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	adminServiceMethods := admin.File_admin_admin_proto.Services().ByName("AdminService").Methods()
	adminServiceListExtensionsHandler := connect.NewUnaryHandler(
		AdminServiceListExtensionsProcedure,
		svc.ListExtensions,
		connect.WithSchema(adminServiceMethods.ByName("ListExtensions")),
		connect.WithHandlerOptions(opts...),
	)
	adminServiceSetExtensionEnabledHandler := connect.NewUnaryHandler(
		AdminServiceSetExtensionEnabledProcedure,
		svc.SetExtensionEnabled,
		connect.WithSchema(adminServiceMethods.ByName("SetExtensionEnabled")),
		connect.WithHandlerOptions(opts...),
	)
	return "/admin.AdminService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AdminServiceListExtensionsProcedure:
			adminServiceListExtensionsHandler.ServeHTTP(w, r)
		case AdminServiceSetExtensionEnabledProcedure:
			adminServiceSetExtensionEnabledHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAdminServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedAdminServiceHandler struct{}

func (UnimplementedAdminServiceHandler) ListExtensions(context.Context, *connect.Request[admin.ListExtensionsRequest]) (*connect.Response[admin.ListExtensionsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("admin.AdminService.ListExtensions is not implemented"))
}

func (UnimplementedAdminServiceHandler) SetExtensionEnabled(context.Context, *connect.Request[admin.SetExtensionEnabledRequest]) (*connect.Response[admin.SetExtensionEnabledResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("admin.AdminService.SetExtensionEnabled is not implemented"))
}
